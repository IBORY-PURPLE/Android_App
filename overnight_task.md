# 임무: 손편지 위젯 앱 — 안드로이드 Phase 0 자율 구현 (밤샘 루프)

## 작업 폴더

C:\Users\chewo\TheOne\03\_제품기획·개발\iphon_widget\Android (여기가 cwd·git 대상)

## 매 반복 시작 시 반드시 읽을 것

- 진행 상태: PROGRESS.md (없으면 생성) — 여기부터 읽고 여기에 기록한다.
- 막힌 결정: DECISIONS_NEEDED.md (없으면 생성)
- 제품 결정·원칙: ..\기획서.md (특히 1.7 제품 원칙, 확정 결정 1~10). 충돌 시 기획서가 우선.
- 실행 계획: ..\개발계획.md (안드로이드 Phase 0, "만드는 순서" 0~4단계)
- SDK 57은 신버전이다. 코드 작성 전 반드시 https://docs.expo.dev/versions/v57.0.0/ 에서
  해당 API 실제 문서를 확인한다. API를 기억으로 지어내지 않는다.

## 최우선 규칙

1. 오버엔지니어링 금지. 기획서 원칙 6("혼자 출시 가능한 최소 제품")이 상한선.
   추상화·설정옵션·미래대비 코드 만들지 마라.
2. 한 반복 = 검증 가능한 "가장 작은 다음 한 걸음". 큰 기능을 한 번에 만들지 않는다.
3. 검증 안 되면 완료 아님. 아래 검증 게이트를 통과해야만 커밋한다.
4. 막히면 추측하지 말고 기록하고 넘어간다. 제품 결정이 애매하거나 유료계정·실물편지·
   사람 판단이 필요하면 DECISIONS_NEEDED.md에 적고 그 작업은 건너뛴 뒤 다른 걸 진행한다.

## 이번 루프의 범위 (벗어나지 말 것)

개발계획.md의 "1단계(앱 골격 + 이미지 획득)"를 완성하고, 2·3단계는 뼈대만 스캐폴드한다.

- 완성 목표(1단계): 편지함(목록/상세) · expo-image-picker로 갤러리에서 편지 사진 선택 ·
  expo-sqlite 로컬 저장 · 편지 메타(애칭·날짜) 입력 · 보기모드 3종 뼈대.
  (1단계는 Expo Go에서 도는 범위로 유지 — 네이티브 개발 빌드가 필요한 작업은 뼈대까지만.)
- 스캐폴드만(2·3단계): OpenCV 세그멘테이션과 Glance 위젯은 화면 자리·인터페이스·TODO 주석까지만.
  실제 파라미터 튜닝은 실물 편지 사진(0단계)이 있어야 하므로 하지 않는다.
- 범위 밖(하지 말 것): Play 제출, 결제, EAS submit, ..\ios 폴더 수정,
  회신 백엔드(Phase 1), git push·원격 전송.

## 반복 프로토콜 (= 피드백 루프)

각 깨어남에서:

1. PROGRESS.md와 DECISIONS_NEEDED.md를 읽어 현재 위치를 파악한다.
2. 남은 것 중 가장 작은 다음 한 걸음 하나를 고른다.
3. 필요하면 docs.expo.dev/versions/v57.0.0 에서 해당 API를 확인한다.
4. 최소 증분으로 구현한다.
5. 검증 게이트를 실행한다:
   - npx tsc --noEmit (타입 에러 0)
   - npx expo-doctor (통과)
   - npx expo export -p android (번들 무에러)
6. 실패하면 고쳐서 다시 검증한다(fix-forward). 3회 시도해도 안 되면
   DECISIONS_NEEDED.md에 원인을 적고 이 증분을 되돌린 뒤 다른 작업으로 넘어간다.
7. 통과하면 커밋한다(작고 명확한 메시지, 한 증분 = 한 커밋).
8. PROGRESS.md를 갱신한다: 방금 한 것 / 검증 결과 / 다음 후보 / 지금까지 반복 횟수.
9. 검증 통과한 증분을 1개 이상 완료하고 PROGRESS.md를 최신으로 남긴 뒤 이 턴을 종료한다.

## 검증 게이트 (커밋 전 필수, 셋 다 통과해야 커밋)

- npx tsc --noEmit 통과
- npx expo-doctor 통과
- npx expo export -p android 무에러

## 안전장치

- 삭제 금지: ..\ios, ..\기획서.md, ..\개발계획.md, 기존 .git 히스토리.
- 네트워크 부작용 금지: git push·배포·제출·외부 전송 일절 금지.
  (npm/expo install 과 docs.expo.dev 문서 fetch 는 허용)
- 비용 금지: 유료 API·결제·계정 생성 금지.

## 스코프 완료 또는 루프 종료 시

MORNING_REPORT.md 작성:

- 활동 핵심 요약(오늘 한 일, 내일 할 일, 막힌 결정)
- 완성된 것(검증 근거·커밋 목록과 함께)
- 검증 못 한 것 / 사람이 눈으로 봐야 할 것
- DECISIONS_NEEDED.md 요약(막힌 제품 결정)
- 내일 아침 이어서 할 첫 걸음
